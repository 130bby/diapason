<h1>FormationElement list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Etablissement</th>
            <th>Diplome</th>
            <th>Annee</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('formationelement_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.etablissement }}</td>
            <td>{{ entity.diplome }}</td>
            <td>{{ entity.annee }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('formationelement_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('formationelement_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('formationelement_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
